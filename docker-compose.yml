services:
  db:
    image: postgres:16-alpine
    container_name: opsflow_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-opsflow_core}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-opsflow_core}"]
      interval: 5s
      timeout: 3s
      retries: 20

  backend:
    build:
      context: ./backend
    container_name: opsflow_backend
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-opsflow_core}?schema=public
      PORT: ${BACKEND_PORT:-3001}
      JWT_SECRET: local-development-secret-change-me-32chars
      JWT_EXPIRES_IN: 8h
      CORS_ORIGINS: http://localhost:5173
      ADMIN_EMAIL: admin@opsflow.local
      ADMIN_PASSWORD: Admin@123
      MASTER_EMAIL: owner@opsflow.local
      MASTER_PASSWORD: Master@123
      GATE_EMAIL: operator@opsflow.local
      GATE_PASSWORD: Gate@123
      LEITOR_EMAIL: scanner@opsflow.local
      LEITOR_PASSWORD: Leitor@123
      COMISSAO_EMAIL: governance@opsflow.local
      COMISSAO_PASSWORD: Comissao@123
      GATE_START_HOUR: 0
      GATE_END_HOUR: 23
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${BACKEND_PORT:-3001}:3001"
    volumes:
      - backup_data:/app/backups

  frontend:
    build:
      context: ./frontend
    container_name: opsflow_frontend
    environment:
      VITE_API_URL: http://localhost:${BACKEND_PORT:-3001}
    depends_on:
      - backend
    ports:
      - "${FRONTEND_PORT:-5173}:5173"

volumes:
  postgres_data:
  backup_data:
